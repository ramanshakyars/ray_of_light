import 'package:flutter/material.dart';

class TalkToLiteScreen extends StatelessWidget {
  const TalkToLiteScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Talk to Lite'),
        centerTitle: true,
      ),
      body: const Center(
        child: Text(''),
      ),
    );
  }
}